fastapi
uvicorn[standard]
pandas
openpyxl

# DATABASE_URL 환경변수를 설정해 DB 모드로 서버를 돌리거나,
# db/migrate_to_postgres.py, db/seed_fake_users.py를 실행할 때 필요합니다.
# DATABASE_URL을 설정하지 않으면(CSV 모드) 이 패키지 없이도 서버는 정상 동작합니다.
psycopg2-binary
